import {
  useCombobox,
  useDisclosure,
  useDocumentTitle,
  useLocalStorage,
  useMediaQuery,
  useMounted,
  useViewportSize,
} from './mantine';

export const useBaseCombobox = useCombobox;
export const useBaseDisclosure = useDisclosure;
export const useBaseDocumentTitle = useDocumentTitle;
export const useBaseLocalStorage = useLocalStorage;
export const useBaseMediaQuery = useMediaQuery;
export const useBaseMounted = useMounted;
export const useBaseViewportSize = useViewportSize;

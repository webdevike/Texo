# Pitfall ledger

Discovery mode. Each row is a hypothesis the contracts must survive, the probe that attacks it,
and what happened. A FAIL is the goal: it names a contract hole before the real build.

| # | Hypothesis | Probe | Result | Contract implication |
|---|---|---|---|---|
| P1 | A schema-driven form needs no per-field code | Drive every field kind in the browser | FAIL then fixed: Mantine NumberInput emits a string mid-typing; server 422'd `"3"` | UI adapters must coerce to the contract's wire types; contract was right to reject |
| P2 | Transport adds no semantics | http→sqlite runs the same suite as sqlite | PASS 10/10 | Suite is the proof; keep it adapter-agnostic |
| P3 | Adding a field to a live entity just works | Add `dueDate` to `issue` with existing sqlite data | FAIL: `list` silently returned old rows, `create` died with raw `SQLiteError: table issue has no column named dueDate` (500, not a contract error) | FIXED: `Store.migrate(entity)` is now in the contract with `StoreSchemaError`; additive changes reconcile, required-without-default and retype refuse. Two suite tests added; memory passes trivially, which is why the suite must run over a persistent adapter |
| P3a | A prepared-statement cache survives DDL | Same probe, after the ALTER fix | FAIL: bun:sqlite `db.query()` caches by SQL string, so `SELECT *` prepared before `ALTER TABLE` returned the old column set; new defaulted column read as false | FIXED with `db.prepare()`. Adapter-local, but the CLASS is contract-relevant: any adapter with a schema cache must invalidate on migrate. Suite catches it |
| P3c | The wire carries the entity, not just its name | http→sqlite runs the evolution tests | FAIL: `storeHandler` resolved `entities[name]` from the server registry, so a client-side shape never reached the server | CLOSED: server is schema authority. `EntitySpec` is now a serializable JSON shape and zod is DERIVED from it (`defineEntity(spec)`), so specs live as files, travel over the wire, and the admin/agent/human edit the same artifact. `Store` (host, owns `migrate`) split from `ClientStore` (UI/transport). Suite split the same way |
| P3b | Removing or retyping a field is also reconciled | Drop a field; add required-without-default to an empty table | PASS: removed field's column stays but reads project only declared fields (memory store needed a `project()` for this, a real contract clause); required field on an EMPTY table is accepted. Both in the suite (14 Store tests) | "Reads return only declared fields" is now contract text |
| P5 | Admin needs zero entity/adapter-specific code | Strapi-style admin at `/admin` reading only `/api/_texo/manifest` | PASS: created `customer` (string, optional string, enum) from the builder → `customer.json` written, table created, row created in Content, app grew a `customer` tab with no code change. `admin/` has no entity or adapter names | Manifest = entities (specs) + adapter labels + system entities. Kept read-only |
| P5a | Destructive schema edits are refused from the admin, not applied | Add required `seats` with no default while a row exists; then give it a default | PASS: first save → 409 with the verbatim `StoreSchemaError` in the builder, nothing written; second save → column added, spec written | The refusal path is what makes the builder safe to hand to an agent |
| P6 | Settings (theme) can dogfood the Store contract | Theme is a row of `_setting` (system entity); change color/radius/scheme/name, reload admin, open app | PASS: persisted through `store.create/update`, survived reload, app rendered dark + new primary + new name | Framework-owned entities are ordinary entities with a `_` prefix, hidden from Content. Spec name regex had to allow the prefix |
| P6a | Admin UI is driveable by an agent | Drove the builder headlessly | Two Mantine traps, both known classes: controlled inputs need the native value setter + `input` event, SegmentedControl radios are visually hidden (click the label). NumberInput string emit (P1) again | UI adapter concern, not contract. Worth a `ui/testing.ts` helper before the blind-agent probe |
| P6b | Texo's existing theme system drops into the admin as-is | Ported `TexoThemeProvider` (574 LOC) + `theme-controls` inspector (718 LOC) into `theme/`, persisted the whole `TexoThemeConfig` + preset as the `_setting` row | PASS after two changes: (1) provider gained `initial` + `onChange` so a host can seed from and write back to the Store; (2) Bun's HTML-route bundler does not resolve CSS modules (`import_theme_components_module is not defined`), so the two `.module.css` files became one global `theme/theme.css` with `texo-` prefixed classes | The `@texo/ui` `Base*` alias layer is a 30-line shim here (`theme/components.ts`); the theme provider was the only real dependency. The Texo repo's `packages/ui` should split into `theme/` (real) and `components.ts` (alias) so consumers do not drag the alias layer |
| P4 | FieldMeta's 4 kinds cover a real app | Hostile entity: relation, date, string[], json, long text | pending | |
| P7 | Store contract does not leak sqlite | Blind subagent writes a third adapter with different semantics (async, eventually consistent) from contract + suite + reference only | pending | |
| P8 | UI never needs capabilities the Store hides | Table at 10k rows, search, pagination | pending | |
| P9 | A blind agent can extend the admin from contracts alone | Blind subagent adds a panel; count guesses | pending | |
| P10 | The component registry can describe any Texo primitive | Register `TexoFieldList` via `defineTexoComponent` for the Custom page | FAIL (expected): `TexoPropertyDefinition` is flat scalars (string/number/boolean/enum/color/icon), so `items: TexoFieldListItem[]` cannot be declared or edited in the inspector. The demo owns fixed data and exposes only knobs (`showBadges`, `editable`, `addLabel`) | Same gap as the entity model: no composite kind. A `json`/array property kind (or a `group` field kind) would fix both; decide once, apply to `FieldSpec` and `TexoPropertyDefinition` together |
| P11 | Field order is a contract concern | Reorder in the builder, save, reload | PASS: `EntitySpec.fields` is already ordered, reorder round-trips through `PUT /specs` and drives table columns + form order with no contract change | Added to contract text: fields render in spec order |
